import { styled } from "../../../../styled-system/jsx";

export const Container = styled("div", {
  base: {
    zIndex: "10",
    position: "fixed",
    bottom: "8",
    left: "50%",
    transform: "translateX(-50%)",
  },
});
