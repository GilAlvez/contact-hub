import { styled } from "../../../styled-system/jsx";

export const PageLayout = styled("section", {
  base: {
    w: "full",
    maxW: "sm",
    m: "auto",
    px: "4",
    sm: {
      maxW: "lg",
    },
  },
});
